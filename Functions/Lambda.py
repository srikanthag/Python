# 1. waf lambda expression to check if the given number is even or not
# even = lambda num: num % 2 == 0
# print(even(5))

# 2. waf lambda function that multiplies two numbers
# multiple = lambda num1, num2: num1 * num2
# print(multiple(2, 4))

# 3. waf lambda fun that returns last element of the sequence
# sequence = lambda seq: seq[-1]
# print(sequence([1,2,3]))

# sequence = lambda se: str(se)[-1]
# print(sequence(1234))

# 4. wa lambda function that checks if the given string is palindrome or not
# pali = lambda string: 'Palindrome' if string == string[::-1] else 'Not Palindrome'
# print(pali('mom'))

# 5. wap that check weather number is even or odd and print number in both cases
# number= lambda num: f"{num} even number" if num % 2 == 0 else f"{num} odd number"
# print(number(9))
